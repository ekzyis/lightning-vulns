__pycache__/
*.py[cod]
.venv/
venv/

# Raw HTTP responses kept by the scraper; safe to delete at any time.
.cache/

# Scraper output. Generated by `python3 scrape.py`, so it is not tracked yet.
# Drop this line once a rendered site needs the dataset committed.
data/
